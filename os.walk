#!/usr/bin/python3
import os

path="/root/test/"

#print(list(os.walk(path)))

print("----------------------")
#for each in list(os.walk(path)):
#     print(each)


for r,d,f in list(os.walk(path)):
    print(r,d,f)
